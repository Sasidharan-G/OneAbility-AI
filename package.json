{
  "name": "oneability-ai",
  "version": "1.0.0",
  "private": true,
  "description": "OneAbility AI: accessibility-first simulated digital payment assistant (college project). Simulation only.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "e2e": "node frontend/tests/e2e/e2e.mjs",
    "test": "node --test \"frontend/tests/*.test.mjs\"",
    "test:all": "node --test \"frontend/tests/*.test.mjs\" && cd backend && .venv/Scripts/python -m pytest -q"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "puppeteer-core": "^25.11.0",
    "qrcode": "^1.5.4"
  }
}
